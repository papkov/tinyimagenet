# @package _global_

augmentation:
  train: config/augmentation/pretrained_resnet/train.yaml
  valid: config/augmentation/pretrained_resnet/valid.yaml

train:
  epochs: 30
  freeze_epochs: 2
  warmup_epochs: 4

resnet: 50

model:
  _target_: torchvision.models.resnet.resnet${resnet}
  pretrained: true
