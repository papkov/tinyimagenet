# @package _global_
experiment: pretrained_resnet

train:
  epochs: 30
  freeze_epochs: 2
  warmup_epochs: 4

resnet: 50

model:
  _target_: torchvision.models.resnet.resnet${resnet}
  pretrained: true
